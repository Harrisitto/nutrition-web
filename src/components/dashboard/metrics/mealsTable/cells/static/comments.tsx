import { cellStyles } from "../defaultStyles";

export const HeaderComments = () => {
  return <div className={cellStyles.longHeader.background}>Comments</div>;
};

export const RowComments = () => {
  return <div className={cellStyles.leftColumnCell.background}>For</div>;
};