import * as Actions from "./setupProfile/actions"
import * as Text from "./setupProfile/text"
import * as Hooks from "./setupProfile/hooks"
import { Provider, Fields } from "./setupProfile/provider"


export const IdxSetUpProfile = {
    Actions,
    Text,
    Hooks,
    Provider,
    Fields,
}